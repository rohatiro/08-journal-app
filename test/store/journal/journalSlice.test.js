import { journalSlice } from "../../../src/store/journal/journalSlice";

describe('Pruebas en JournalSlice', () => {
    test('debe de regresar el estado inicial y llamarse journal', () => {
        // const state = journalSlice.reducer(checkingState, {});
        
        // expect(state).toEqual(checkingState);
        // expect(journalSlice.name).toBe('auth');
    })
})